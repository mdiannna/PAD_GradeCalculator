def CircuitBreaker():
	def __init__(address):
		self.address = address

	def request(params):
		if self.tripped:
			remove_from_cahce
			raise( "Error.... Trapped etc TODO")
			return  #TODO

		endpoint = self.address + params[:path]

		# RestClient::Request.execute(params.merge(url:endpoint))
		r = requests.get(endpoint, params)
    	
    	rescue errno:ErrorConfused???
    	result = cache.current.increase(redis_key)

    	if result > 3:
    		self.tripped = True

    def redis_key():
		# TODO    	

	def remove_from_cache():
		#TODO
